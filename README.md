# FoodBridge — Fresh Tab

Monorepo (server + client) with Node 20 LTS, TypeScript, Express, Vite/React.
- Run `npm install` at repo root
- Put your API env in `server/.env` (see `server/.env.example`)
- Dev: `npm run dev` (server :8080, client :5173)

$BASE = 'https://foodbridge-server-rv0a.onrender.com'
$u = "christian"
$items = @{ items = @(@{ type="recipe"; title="Pasta"; sourceUrl="https://ex"; durationSec=900 }); userId=$u } | ConvertTo-Json
iwr "$BASE/api/cart/upsert" -Method POST -Body $items -ContentType "application/json" | % Content
